package org.example.model.GameInfo;

import org.example.model.User;

public class Workers extends People {

    public Workers(String type, Integer speed, User owner) {
        super(type, owner);
    }
}
